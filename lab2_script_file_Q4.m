clc,clear all, close all;
% Example usage
numerator = [1, 2, 3]; % Example numerator coefficients
denominator = [1, 0.5, 0.25]; % Example denominator coefficients
check_stability(numerator, denominator);
